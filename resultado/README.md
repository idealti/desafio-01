## Classes CSS 

navContainer = nc
banner = b
formContainer = fc
description = d

                "nome": `"${data.name.value}"`,
                "email": `"${data.email.value}"`,
                "msg": `"${data.msg.value}"`